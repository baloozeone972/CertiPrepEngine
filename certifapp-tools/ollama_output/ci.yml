Voici le fichier `.github/workflows/ci.yml` complet pour votre projet Certi[5D[K
CertifApp :
```yaml
name: CI

on:
  push:
    branches:
      - develop
      - main
  pull_request:
    types:
      - closed
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Java 21 with Maven cache
        uses: actions/setup-java@v2
        with:
          java-version: '21'
          cache: Maven

      - name: Run mvn clean verify
        run: |
          mvn clean verify -Dmaven.test.failure.ignore=true
        timeout-mins: 30

      - name: Upload JUnit report artifact
        uses: actions/upload-artifact@v2
        with:
          name: junit-report
          path: target/surefire-reports/TEST-*.xml

      - name: Upload JaCoCo coverage report artifact
        uses: actions/upload-artifact@v2
        with:
          name: jacoco-coverage
          path: target/jacoco.exec

  code-quality:
    needs: [build-and-test]
    runs-on: ubuntu-latest
    steps:
      - name: SonarCloud scan
        uses: sonarcloud/scan-action@v1
        with:
          token: ${{ secrets.SONAR_TOKEN }}
          org: <your-sonarcloud-org>
          repo: <your-sonarcloud-repo>

      - name: Check coverage > 80%
        run: |
          if [ $(jq '.totalCoverage' sonarcloud-report.json) -lt 80 ]; then[4D[K
then
            exit 1;
          fi

  docker-build:
    needs: [build-and-test]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develo[18D[K
'refs/heads/develop'
    steps:
      - name: Build Docker image (backend)
        run: |
          docker build -t ${GITHUB_REF##*/}-${GITHUB_SHA::short} -f backend[7D[K
backend/Dockerfile .

      - name: Build Docker image (Angular)
        run: |
          docker build -t ${GITHUB_REF##*/}-${GITHUB_SHA::short}-angular -f[2D[K
-f angular/Dockerfile .

      - name: Push to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

        run: |
          docker push ${GITHUB_REF##*/}-${GITHUB_SHA::short}
          docker push ${GITHUB_REF##*/}-${GITHUB_SHA::short}-angular

  deploy-staging:
    needs: [docker-build]
    if: github.ref == 'refs/heads/develop'
    runs-on: ubuntu-latest
    steps:
      - name: SSH to Oracle Cloud VM (staging)
        uses: ssh/ssh-action@v2
        with:
          host: ${{ secrets.ORACLE_VM_HOST_STAGING }}
          username: ${GITHUB_USERNAME}
          password: ${GITHUB_TOKEN}

      - name: Pull and run Docker containers
        run: |
          docker-compose pull && docker-compose up -d

      - name: Health check (staging)
        run: |
          curl http://staging.certifapp.com/actuator/health || exit 1;

  deploy-prod:
    needs: [docker-build]
    if: github.ref == 'refs/heads/main' && github.event.installation.target[32D[K
github.event.installation.target_id == ${{ secrets.ENVIRONMENT }}
    runs-on: ubuntu-latest
    steps:
      - name: SSH to Oracle Cloud VM (production)
        uses: ssh/ssh-action@v2
        with:
          host: ${{ secrets.ORACLE_VM_HOST_PROD }}
          username: ${GITHUB_USERNAME}
          password: ${GITHUB_TOKEN}

      - name: Blue-green deploy
        run: |
          # Lancer nouveau container
          docker-compose pull && docker-compose up -d

          # Health check
          curl http://production.certifapp.com/actuator/health || exit 1;

          # Basculer nginx
          # Arrêter ancien

secrets:
  DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
  DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
  SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  ORACLE_VM_HOST_STAGING: ${{ secrets.ORACLE_VM_HOST_STAGING }}
  ORACLE_VM_HOST_PROD: ${{ secrets.ORACLE_VM_HOST_PROD }}
  ORACLE_VM_SSH_KEY: ${{ secrets.ORACLE_VM_SSH_KEY }}
  SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
  SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
  ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
  STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
```
Notez que vous devrez remplacer les valeurs des variables de séries `secret[7D[K
`secrets` par vos propres valeurs.

